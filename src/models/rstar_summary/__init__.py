"""Every r* this repo estimates, gathered on one nominal scale.

Not a model. It estimates nothing and adds nothing to the evidence. It loads
the other packages' saved runs, refreshes any that are not from today, and
charts them together so the DISAGREEMENT between them is visible in one place.

Each of those models anchors r* to a different thing and returns roughly what
its anchor implies, so the spread between the lines is four structural
assumptions rather than sampling error. Nothing here picks a winner.
"""
